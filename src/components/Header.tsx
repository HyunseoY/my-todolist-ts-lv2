import styled from 'styled-components';

const Header = () => {
  return <StHeader>T O D O</StHeader>;
};

export default Header;

const StHeader = styled.header`
  background: none;
  color: white;
  font-weight: bold;
  font-size: 45px;
  height: 70px;
  padding-right: 665px;
`;
